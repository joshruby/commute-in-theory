# Commute Recording Utility

Commutes are recorded by scheduled AWS Lambda function invocations every 15 minutes.

Commute stats are recomputed by scheduled AWS Lambda function invocations every week.